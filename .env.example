# AURA Configuration Template
# Copy this file to .env and fill in your actual values

# AI Provider Configuration
API_KEY="YOUR_GOOGLE_AI_KEY_HERE"
API_PROVIDER="google"
DEFAULT_MODEL="gemini-2.5-flash"

# Server Configuration
PORT=8000

# Optional: Custom AI Personality
AI_NAME="AURA"
AI_PERSONALITY="personal_assistant"

# Optional: Extended System Prompt (uncomment to customize)
# SYSTEM_PROMPT="You are AURA, with access to knowledge graph tools (query_graph, create_entities, add_observations) and agent commands (agent_command). Use these tools proactively when users need information retrieval or storage."
